// Code generated by __generator__/interpreter.go at once

package builtin

import (
	"crypto/hmac"
	"crypto/sha256"
	"encoding/base64"
	"encoding/hex"

	"github.com/ysugimoto/falco/interpreter/context"
	"github.com/ysugimoto/falco/interpreter/function/errors"
	"github.com/ysugimoto/falco/interpreter/value"
)

const Digest_hmac_sha256_with_base64_key_Name = "digest.hmac_sha256_with_base64_key"

var Digest_hmac_sha256_with_base64_key_ArgumentTypes = []value.Type{value.StringType, value.StringType}

func Digest_hmac_sha256_with_base64_key_Validate(args []value.Value) error {
	if len(args) != 2 {
		return errors.ArgumentNotEnough(Digest_hmac_sha256_with_base64_key_Name, 2, args)
	}
	for i := range args {
		if args[i].Type() != Digest_hmac_sha256_with_base64_key_ArgumentTypes[i] {
			return errors.TypeMismatch(Digest_hmac_sha256_with_base64_key_Name, i+1, Digest_hmac_sha256_with_base64_key_ArgumentTypes[i], args[i].Type())
		}
	}
	return nil
}

// Fastly built-in function implementation of digest.hmac_sha256_with_base64_key
// Arguments may be:
// - STRING, STRING
// Reference:
func Digest_hmac_sha256_with_base64_key(ctx *context.Context, args ...value.Value) (value.Value, error) {
	// Argument validations
	if err := Digest_hmac_sha256_with_base64_key_Validate(args); err != nil {
		return value.Null, err
	}

	keyStr := value.Unwrap[*value.String](args[0])
	if keyStr.Value == "" {
		return value.Null, nil
	}
	input := value.Unwrap[*value.String](args[1])

	// Decode base64 key
	key, err := base64.StdEncoding.DecodeString(keyStr.Value)
	if err != nil {
		return value.Null, err
	}

	mac := hmac.New(sha256.New, key)
	mac.Write([]byte(input.Value))

	return &value.String{
		Value: "0x" + hex.EncodeToString(mac.Sum(nil)),
	}, nil
}
