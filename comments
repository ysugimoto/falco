TODO:

sub get_int INTEGER {
  declare local var.tmp BOOL;
  set var.tmp = true;
  return var.tmp  // invalid, could not specify variable
}

Fiddle: expected ;
Falco: missing ;


sub get_int INTEGER {
  declare local var.tmp INTEGER;
  set var.tmp = 10;
  return var.tmp;  // compiles as expected
}

Fiddle: check
Falco:

sub get_int INTEGER {
  return (100); // invalid, literal could not enclose in parenthesis
}

sub get_str STRING {
  declare local var.tmp STRING;
  set var.tmp = "foo";
  return var.tmp "bar";
}

 // Expected ;, got "bar"

DONE:

sub vcl_recv BOOL {  // State-machine methods may not have a return type
  ...
}
